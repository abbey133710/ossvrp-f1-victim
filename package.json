{
  "name": "ossvrp-f1-victim",
  "version": "1.0.0",
  "private": true,
  "description": "Benign baseline app for the OSS-VRP F1 workflow_run replica. The vulnerable chained.yml checks out the PR-head repo, not this one, so this file is only here so the repo looks like a real npm project.",
  "scripts": {
    "build": "echo benign build",
    "test": "echo benign tests"
  }
}
